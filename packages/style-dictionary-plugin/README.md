# @dtcg-formulas/style-dictionary-plugin

> Stub — not yet implemented.

Style Dictionary integration that reads `org.dtcg-formulas` extensions during token resolution, validates call expressions against the registry, and optionally re-resolves `$value` from formulas.
