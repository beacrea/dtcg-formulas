# @dtcg-formulas/registry

> Stub — not yet implemented.

Function registry implementing the contract in `packages/spec/registry-contract.md`. Stores parsed `.module.scssdef` declarations and custom function adapters.
